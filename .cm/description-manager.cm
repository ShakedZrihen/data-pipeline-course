# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  update_description:
    if:
      - {{ pr.description != 'hello' }}
    run:
      - action: update-description@v1
        args:
          description: 'hello'
